const Header = ( ) => {
    return (
      <div className="header">
        <h1>Weather App</h1>
        <p> To find out the weather of different cities, type it in the search bar</p>
      </div>
    )
  }

  export default Header;